<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.fcr.infrastructure.dao.ICategoryDao">

    <select id="getCategoryProductCount" resultType="java.util.Map">
        SELECT c.name, COUNT(p.id) as product_count FROM category c LEFT JOIN product p ON c.id = p.category_id WHERE c.status = 1 GROUP BY c.id, c.name ORDER BY product_count DESC
    </select>

</mapper>