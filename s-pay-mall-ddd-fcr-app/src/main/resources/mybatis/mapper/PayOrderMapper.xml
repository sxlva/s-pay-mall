<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.dao.IOrderDao">

    <insert id="insert" parameterType="cn.bugstack.infrastructure.dao.po.PayOrder">
        insert into pay_order(user_id, product_id, product_name, order_id, order_time,
                              total_amount, status, create_time, update_time)
        values(#{userId}, #{productId}, #{productName}, #{orderId}, #{orderTime},
               #{totalAmount}, #{status}, now(), now())
    </insert>

    <select id="queryUnPayOrder" parameterType="cn.bugstack.infrastructure.dao.po.PayOrder" resultType="cn.bugstack.infrastructure.dao.po.PayOrder">
        select product_id, product_name, order_id, order_time, total_amount, status, pay_url
        from pay_order
        where user_id = #{userId} and product_id = #{productId}
        order by id desc
            limit 1
    </select>

</mapper>